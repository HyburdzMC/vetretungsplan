const DSB = require('./index');

console.log(DSB);

const client = new DSB('277162', 'PlanMCG');

(async () => {
	console.log(DSB.findMethodInData('timetable', await client.fetch()));
	console.log(await client.fetchV1());
})().catch(console.trace);
